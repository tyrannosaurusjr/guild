{
  "name": "guild",
  "version": "1.0.0",
  "private": true,
  "description": "Shared schema, guild page, and cross-site linking for all guild sites.",
  "scripts": {
    "build": "npx tsx src/build.ts"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
